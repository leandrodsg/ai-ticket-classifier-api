[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "frankenphp php-server --workers 4 --listen :$PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3
